# مستندات و قوانین جامع سیستم مدارس، مسابقات و بازی‌های قربانی کیدز (Ghorbani Kids B2B)

این سند مرجع تمامی استانداردها، معماری‌ها و منطق‌های پیاده‌سازی‌شده در سیستم مدارس و مسابقات است و باید در تمامی توسعه‌های آینده دقیقاً رعایت شود:

---

## ۱. ایزولاسیون کامل ماژول مدارس (Modular Isolation)
- تمامی امکانات، جداول، کلاس‌ها و توابع سیستم مدارس به صورت ۱۰۰٪ مستقل در پوشه زیر نگهداری می‌شوند:
  `wp-content/mu-plugins/ghorbanikids-schools/`
- هیچ تغییری نباید باعث تداخل یا وابستگی نامناسب به کدهای قبلی سایت شود.

---

## ۲. پنل مدیریت مهدکودک و مدارس (`/school-panel/`) — ۱۰۰٪ بدون رفرش (Zero-Reload SPA)
- تمامی عملیات‌های پنل مدارس باید به صورت **کاملاً آسنکرون (AJAX) و بدون رفرش صفحه** انجام شوند:
  1. **ثبت دانش‌آموز جدید:** ردیف جدید فوراً در بالای جدول درج شده، چشمک سبز می‌زند و فوکوس به ورودی نام برمی‌گردد.
  2. **ویرایش و حذف دانش‌آموز:** درجا ردیف جدول بروزرسانی یا حذف می‌شود.
  3. **ثبت، ویرایش و حذف کلاس:** کارت‌ها و تب‌های فیلتر در لحظه آپدیت می‌شوند.
  4. **ذخیره برند و لوگو:** هدر پنل درجا تغییر می‌کند.
- **حفظ فیلتر فعال کلاس:** با ثبت یا ویرایش دانش‌آموز، فیلتر کلاس انتخابی فعال باقی می‌ماند و پاک نمی‌شود.
- **جلوگیری از ارسال با اینتر:** زدن کلید اینتر در فرم‌ها نباید باعث رفرش صفحه شود.

---

## ۳. هویت دانش‌آموز و اولویت قطعی بر اکانت وردپرس (Student Priority Mode)
- ورود به هر بازی یا آزمون از طریق توکن دانش‌آموز (`st_token=ST-XXXXX`):
  1. **اولویت قطعی با نوآموز:** حتی اگر کاربر با اکانت ادمین یا مادر در وردپرس لاگین باشد، هویت بازی و ثبت رکورد ۱۰۰٪ به نام دانش‌آموز ثبت می‌شود.
  2. **بازگشایی خودکار قفل بازی‌های VIP:** دانش‌آموزان ثبت‌شده در مدارس به تمام بازی‌های قفل‌دار دسترسی کامل دارند (Bypass Paywall).
  3. **پر شدن خودکار فرم آزمون‌ها:** نام و سن کودک در آزمون‌ها به صورت Readonly پر شده و نشان مهد نمایش داده می‌شود.
  4. **پایان بازی و تبریک:** پاپ‌آپ شاداب کودکانه امتیاز ثبت‌شده در لیگ کلاسی را تبریک می‌گوید.

---

## ۴. پاکیزگی منوها و پیمایش عمومی سایت (Clean Navigation Rule)
- **عدم دستکاری منوی اصلی سایت:** پیوندهای منوی هدر و ناوبری عمومی سایت (`/games/` و لینک‌های منو) هرگز نباید پارامتر `st_token` یا `league_code` به خود بگیرند.
- **پیمایش عمومی عادی:** کاربرانی که از منوی سایت وارد بازی‌ها می‌شوند، کاملاً عادی و بدون بنر یا توکن بازی می‌کنند.
- **انتقال توکن فقط به آی‌فریم بازی:** توکن دانش‌آموز در صفحات اختصاصی فقط به سورس آی‌فریم داخلی بازی متصل می‌شود.
- **عدم نمایش بنرهای مزاحم:** هیچ نوار شناور اضافی روی صفحات بازی‌ها نمایش داده نمی‌شود.

---

## ۵. موتور لیگ‌ها و مسابقات اختصاصی کلاسی (Custom Tournaments Engine)
- **ایجاد مسابقه در پنل معلم (`/teacher-class/`):**
  - معلم عنوان مسابقه، بازی‌های منتخب (۱ یا چند بازی) و **مهلت زمانی انقضا** (۶ ساعت، ۱۲ ساعت، ۲۴ ساعت، ۴۸ ساعت، ۳ روز، ۱ هفته، یا نامحدود) را تعیین می‌کند.
- **سالن مسابقه ایزوله (`/league/?code=LG-XXXXX`):**
  - فقط بازی‌های منتخب معلم در سالن مسابقه نمایش داده می‌شوند.
  - اگر با لینک عمومی باز شود، والدین از منوی بازشو نام فرزندشان را انتخاب می‌کنند.
  - اگر با لینک اختصاصی (`st_token`) باز شود، کارت آمار دانش‌آموز (بهترین رکورد، مجموع امتیازات مسابقه، رتبه، دفعات شرکت) نمایش داده می‌شود.
  - در پایان بازی فقط دو دکمه «🏆 بازگشت به سالن مسابقه» و «🔄 بازی مجدد برای رکورد بالاتر» نمایش داده شده و هیچ لینک خروجی به کاتالوگ عمومی داده نمی‌شود.
  - با پایان مهلت زمانی (`expires_at`)، بازی‌ها قفل شده ولی جدول رتبه‌بندی نهایی برای مشاهده باقی می‌ماند.

---

## ۶. تفکیک بخش‌های پنل کلاسی معلم (`/teacher-class/?code=CLS-XXXXX`)
- پنل معلم به صورت ۳ تب بسیار مرتب و تفکیک‌شده طراحی شده است:
  - 🎓 **تب ۱ (پیش‌فرض بالا):** «لیست نوآموزان و دسترسی‌ها» با جستجوی سریع و ۴ آیکون اختصاصی:
    - 🟢 ارسال لینک اختصاصی به بله مادر
    - ▶️ ورود مستقیم به بازی‌ها
    - 🧠 ورود مستقیم به آزمون‌ها
    - 📋 کپی لینک
  - 🏆 **تب ۲:** «مسابقات و لیگ‌های کلاسی» شامل دکمه ساخت مسابقه، ارسال گروهی به بله، و جدول زنده ارسال اختصاصی مسابقه برای هر نوآموز.
  - 🥇 **تب ۳:** «سکوی قهرمانان کلاس» شامل مدال‌های طلا، نقره و برنز بر اساس امتیاز کل.

---

## ۷. استاندارد ارسال امتیاز بازی‌ها (GK_SUBMIT_SCORE postMessage)
- در هر بازی، در نقطه اتمام بازی باید قطعه کد زیر اجرا شود:
```javascript
if (window.parent) {
    window.parent.postMessage({
        type: 'GK_SUBMIT_SCORE',
        score: finalScore
    }, '*');
}
```
- سیستم امتیاز را به صورت خودکار در موارد زیر ثبت می‌کند:
  1. `gk_students.total_game_score` (لیگ کل کلاسی)
  2. `gk_league_scores` (در صورت حضور در مسابقه فعال)
  3. `gk_game_scores` (تابلوی رکوردهای عمومی زیر صفحه همان بازی)

---

## ۸. سیاست صفحات پویا و بدون کش (No-Cache Policy)
- صفحات `/league/` و `/teacher-class/` به صورت داینامیک و بدون کش سرور لود می‌شوند تا ثبت امتیازات و تغییر رتبه‌ها در همان لحظه و بدون تاخیر نمایش داده شوند.

---

## ۹. اصل اولویت مطلق موبایل و طراحی بی‌نقص و تمیز در گوشی (Mobile-First & Clean Mobile UX Rule)
- طراحی، ریسپانسیو بودن و تجربه کاربری در **گوشی و موبایل اولویت درجه یک** در تمام صفحات سایت است:
  1. **هدر و ناوبری فوق‌العاده سبک در موبایل:** هدر در موبایل باید کاملاً باریک، سبک، بدون کش‌آمدن عمودی و بدون اسکرول افقی باشد.
  2. **مخفی‌سازی هوشمند سایدبار دسکتاپ:** سایدبار دسکتاپ در تمام صفحات موبایل و تبلت به صورت ۱۰۰٪ مخفی باشد و فقط منوی کشویی تمیز (Drawer) با تاچ نرم باز شود.
  3. **فرم‌ها، جداول و دکمه‌های مناسب لمس (Touch-Friendly Full-Width):** در موبایل، فیلدها و دکمه‌های اقدام به صورت تمام‌عرض با ارتفاع مناسب لمس انگشت (حداقل ۴۸ پیکسل) چیده شوند.
  4. **صفر درصد اسکرول افقی ناخواسته (Zero Horizontal Overflow):** هیچ المان، جدول، تصویر یا باکسی نباید باعث لغزش افقی صفحه در موبایل شود.

---

## ۱۱. اصل تصادفی‌سازی و عدم پیش‌بینی‌پذیری گزینه‌ها در بازی‌ها (Game Randomization & Anti-Pattern Rule)
- در تمام بازی‌های آموزشی، آزمون‌ها و تمرین‌ها:
  1. **شافل تصادفی گزینه‌ها:** گزینه‌ها در هر راند و مرحله با الگوریتم Fisher-Yates شافل شوند تا پاسخ صحیح در موقعیت مکانی ثابت یا قابل حدس نباشد.
  2. **تصادفی‌سازی استخر سوالات:** ترتیب پرسش‌ها در هر بار شروع بازی به صورت رندوم چیده شود تا کودک نتواند توالی‌های یکی‌در‌میانی یا ریتمیک را حفظ کند.
  3. **برهم‌زدن جای حروف و دکمه‌ها:** دکمه‌های تعاملی، کارت‌ها و حروف انتخابی در هر نوبت در جایگاه‌های متفاوتی لود شوند.

---

## ۱۲. اصل الزامی طراحی آیکون وکتور (SVG) و گرادیانت اختصاصی برای تمام بازی‌ها (Game Icons & Assets Rule)
- تمام بازی‌های تولیدی یا ثبت‌شده در سایت باید دارای آیکون وکتور (SVG) اختصاصی، رنگارنگ، متناسب با موضوع بازی و پس‌زمینه گرادیانت مدرن در `GK_Game_Assets::get_presets()` باشند و هیچ بازی بدون آیکون گرافیکی باقی نماند.

---

## ۱۳. اصل وقار، اخلاق‌مداری و ادب آموزشی در گزینه‌ها و سناریوهای بازی (Game Ethics & Pedagogy Rule)
- در تمام بازی‌های دینی، قرآنی، هدیه‌ها و بازی‌های مهارتی:
  1. **گزینه‌های انحرافی وزین و مؤدبانه:** گزینه‌های نادرست هرگز نباید شامل رفتارهای سخیف، تمسخرآمیز، بی‌ادبانه یا توهین‌آمیز باشند؛ بلکه باید انتخاب‌های طبیعی و منطقی دیگر باشند.
  2. **آموزش اخلاق و ارزش‌های اسلامی:** سناریوها همواره تقویت‌کننده شکرگزاری، ادب، احترام به والدین و معلمان، پاکیزگی و مهربانی با دوستان و طبیعت باشند.

---

## ۱۴. پروتکل پشتیبان‌گیری و همگام‌سازی خودکار مخزن گیت‌هاب (Automated Full Backup & GitHub Sync)
- با هر بار گفتن عبارت «بکاپ بگیر» یا درخواست پشتیبان‌گیری از سمت کاربر:
  1. اسکریپت خودکار `python backup_and_sync.py` بدون نیاز به اقدام دستی اجرا می‌شود.
  2. خروجی کامل و بدون نقص پایگاه داده MySQL (`database_backup.sql` و نسخه فشرده `database_backup.sql.gz`) از سرور زنده استخراج و در ریشه پروژه ذخیره می‌شود.
  3. تمام ماژول‌های مستقل هسته (`wp-content/mu-plugins/`)، پوسته اختصاصی (`wp-content/themes/ghorbanikids/`) و بیش از ۵۰ بازی وب (`wp-content/games/`) با سرور همگام‌سازی می‌شوند.
  4. تمامی تغییرات در قالب یک کامیت منظم Git ثبت شده و بلافاصله به شاخه اصلی (`main`) در گیت‌هاب Push می‌شوند.

---

## ۱۵. شیوه‌نامه استاندارد چرخه توسعه، اعتبارسنجی و استفاده از گیت (Development Workflow & Git SOP Rule)
در تمامی فرآیندهای توسعه، ویرایش و نگهداری کدهای پروژه:
1. **استفاده از گیت برای مشاهده نسخه‌های قبلی و بکاپ‌ها:** هر زمان نیاز به بررسی نسخه قبلی، مقایسه تفاوت‌ها یا بازگشت تغییرات هر فایلی بود، مستقیماً از تاریخچه گیت (`git log`, `git diff`, `git checkout`) در همین پوشه محلی استفاده شود.
2. **اعتبارسنجی و کامپایل اجباری کدهای PHP قبل از استقرار:** قبل از ارسال هر فایل PHP به سرور زنده، حتماً بررسی نحوی و کامپایل با مفسر لوکال PHP زمپ (`D:\xampp\php\php.exe -l <filepath>`) انجام شود تا از نبود هرگونه خطای سینتکسی، پرانتز باز یا Fatal Error اطمینان ۱۰۰٪ حاصل شود.
3. **بررسی توازن و سلامت CSS و JavaScript:** استایل‌ها از نظر توازن آکولادها، سلامت پکت‌ها و اولویت قرارگیری مدیا کوئری‌ها در انتهای فایل بررسی شوند.
4. **استقرار مستقیم و تخلیه کامل کش:** پس از پاس شدن تست‌های کامپایل، کدها به صورت مستقیم روی هاست دیپلوی شده و کش لایت‌اسپید و وردپرس تخلیه شوند.









