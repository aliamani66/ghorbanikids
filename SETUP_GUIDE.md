# 🚀 راهنمای راه‌اندازی و بازگردانی سریع سایت قربانی کیدز (Full Setup & Restore Guide)

این مخزن شامل **۱۰۰٪ کدهای قالب، ماژول‌ها، ۵۰+ بازی و دیتابیس کامل** سایت قربانی کیدز است. برای راه‌اندازی این سیستم روی یک وردپرس خام یا سرور جدید، تنها ۳ مرحله زیر کافی است:

---

## ۱. کپی کردن فایل‌های پروژه (Files)
محتویات پوشه wp-content/ را در پوشه wp-content/ وردپرس جدید کپی کنید:
- wp-content/themes/ghorbanikids/ (قالب اختصاصی، هدر، فوتر، برگه‌ها و استایل‌ها)
- wp-content/mu-plugins/ (ماژول‌های اختصاصی مدارس، کاتالوگ، آزمون‌ها و اشتراک‌ها)
- wp-content/games/ (بیش از ۵۰ بازی وب تعاملی HTML5)

---

## ۲. ایمپورت پایگاه داده (Database Import)
فایل پایگاه داده database_backup.sql را در دیتابیس وردپرس خود ایمپورت نمایید:
- **از طریق phpMyAdmin:** دیتابیس را انتخاب کرده، تب **Import** را زده و فایل database_backup.sql را بارگذاری کنید.
- **یا از طریق خط فرمان (CLI):**
`ash
mysql -u DB_USER -p DB_NAME < database_backup.sql
`

---

## ۳. به‌روزرسانی آدرس سایت (در صورت تغییر دامنه یا لوکال‌هاست)
اگر سایت را روی لوکال‌هاست یا دامنه‌ای دیگر اجرا می‌کنید، آدرس سایت را در جدول wp_options تنظیم کنید:
`sql
UPDATE wp_options SET option_value = 'http://localhost/ghorbanikids' WHERE option_name IN ('siteurl', 'home');
`

---

## ۴. فعال‌سازی قالب
از پیشخوان وردپرس > **نمایش > پوسته‌ها**، پوسته **قربانی کیدز (GhorbaniKids)** را فعال کنید.

🎉 **تبریک! تمام بخش‌های سایت (پنل مدارس، سالن لیگ‌ها، کاتالوگ ۵۰+ بازی، آزمون‌ها و حساب کاربری) بلافاصله آماده بهره‌برداری است.**
